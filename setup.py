from setuptools import setup

setup(
    name='dataset_split',
    version='0.1',
    description='Splitting the dataset into train, dev and test sets for annotation and features',
    author='Annanda Sousa',
    author_email='annanda.sousa@insight-centre.org',
    packages=['non_sequential_split'],
    zip_safe=False
)
